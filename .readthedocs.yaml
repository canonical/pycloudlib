version: 2

build:
  os: "ubuntu-22.04"
  tools:
    python: "3.10"

formats: all

python:
  install:
    - method: uv
      path: .
      extra-requirements:
        - docs

sphinx:
  configuration: docs/conf.py
